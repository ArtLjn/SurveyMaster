<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.practice.surveymaster.mapper.UserMapper">

    <!-- 基础结果映射 -->
    <resultMap id="BaseResultMap" type="org.practice.surveymaster.model.User">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 基础列 -->
    <sql id="Base_Column_List">
        id, username, password, email, created_at
    </sql>

    <!-- 根据ID查询用户 -->
    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user
        WHERE id = #{id, jdbcType=BIGINT}
    </select>

    <!-- 查询所有用户 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user
        ORDER BY created_at DESC
    </select>

<!--    根据用户名密码查询用户-->
    <select id="selectByUsernameAndPassword" parameterType="org.practice.surveymaster.dto.UserLogin" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user
        WHERE username = #{username, jdbcType=VARCHAR}
          AND password = #{password, jdbcType=VARCHAR}
    </select>

    <!-- 插入用户 -->
    <insert id="insert" parameterType="org.practice.surveymaster.model.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, email, created_at)
        VALUES (#{username, jdbcType=VARCHAR}, #{password, jdbcType=VARCHAR}, 
                #{email, jdbcType=VARCHAR}, #{createdAt, jdbcType=TIMESTAMP})
    </insert>

    <!-- 根据用户名查询用户（用于登录） -->
    <select id="selectByUsername" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user
        WHERE username = #{username, jdbcType=VARCHAR}
    </select>

    <!-- 检查用户名是否存在 -->
    <select id="existsByUsername" parameterType="java.lang.String" resultType="java.lang.Boolean">
        SELECT COUNT(*) > 0
        FROM user
        WHERE username = #{username, jdbcType=VARCHAR}
    </select>

    <!-- 检查邮箱是否存在 -->
    <select id="existsByEmail" parameterType="java.lang.String" resultType="java.lang.Boolean">
        SELECT COUNT(*) > 0
        FROM user
        WHERE email = #{email, jdbcType=VARCHAR}
    </select>

</mapper>